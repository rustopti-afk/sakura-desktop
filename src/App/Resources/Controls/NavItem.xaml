<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="Sakura.NavItem" TargetType="ListBoxItem">
        <Setter Property="Height" Value="{StaticResource Sakura.Size.NavItem}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="8,2"/>
        <Setter Property="Foreground" Value="{StaticResource Sakura.Brush.FgSecondary}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid>
                        <Border x:Name="Rail" Width="3" HorizontalAlignment="Left"
                                CornerRadius="2" Background="Transparent" Margin="0,8"/>
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="8" Padding="16,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                           FontSize="15" VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Text="{Binding IconGlyph}"/>
                                <TextBlock Grid.Column="1" Margin="12,0,0,0"
                                           FontFamily="{StaticResource Sakura.Font.Ui}"
                                           FontSize="13" FontWeight="Medium"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Text="{Binding Label}"/>
                                <Ellipse x:Name="Dot" Grid.Column="2"
                                         Width="5" Height="5"
                                         Fill="{StaticResource Sakura.Brush.AccentGold}"
                                         Opacity="0" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Sakura.Brush.BgOverlay}"/>
                            <Setter Property="Foreground" Value="{StaticResource Sakura.Brush.FgPrimary}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Sakura.Brush.BgRaised}"/>
                            <Setter TargetName="Rail" Property="Background" Value="{StaticResource Sakura.Brush.AccentPink}"/>
                            <Setter TargetName="Dot" Property="Opacity" Value="1"/>
                            <Setter Property="Foreground" Value="{StaticResource Sakura.Brush.FgPrimary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
